# ViewRig Changesets

ViewRig v0.3 configures Changesets for dry-run release governance only.

Do not run `changeset version`, `changeset publish`, `npm publish`, or create release tags unless a later release phase explicitly approves public package publication.
